import 'package:flutter/material.dart';
import 'header_profile_info.dart';

class ProfileTrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // return Stack(
    //   children: <Widget>[HeaderProfileInfo()],
    // );

    return Column(
      children: <Widget>[HeaderProfileInfo()],
    );
  }
}
